# 样式管理

## 5.1 Tailwind CSS 简介

Tailwind CSS 是一个实用优先的 CSS 框架，通过组合工具类来快速构建用户界面。

### Tailwind CSS 的特点

- ✅ **实用优先**：使用工具类而不是自定义 CSS
- ✅ **响应式设计**：内置响应式断点
- ✅ **可定制**：可以自定义主题和配置
- ✅ **性能优化**：只包含使用的样式

---

## 5.2 Tailwind CSS 基础

### 基本语法

```typescript
// 基础类名
<div className="bg-blue-500 text-white p-4 rounded">
  内容
</div>
```

**类名说明：**
- `bg-blue-500`：背景色（蓝色，500 色阶）
- `text-white`：文字颜色（白色）
- `p-4`：内边距（1rem）
- `rounded`：圆角

---

### 常用工具类

**间距：**

```typescript
<div className="p-4 m-2">      {/* padding: 1rem, margin: 0.5rem */}
<div className="px-4 py-2">    {/* padding-x: 1rem, padding-y: 0.5rem */}
<div className="mt-4 mb-2">    {/* margin-top: 1rem, margin-bottom: 0.5rem */}
```

**颜色：**

```typescript
<div className="bg-blue-500 text-white">     {/* 背景蓝色，文字白色 */}
<div className="bg-gray-100 text-gray-800">  {/* 背景浅灰，文字深灰 */}
<div className="border border-gray-300">     {/* 边框灰色 */}
```

**布局：**

```typescript
<div className="flex items-center justify-between">  {/* Flexbox 布局 */}
<div className="grid grid-cols-3 gap-4">            {/* Grid 布局 */}
<div className="container mx-auto">                 {/* 容器居中 */}
```

**尺寸：**

```typescript
<div className="w-full h-64">        {/* 宽度 100%，高度 16rem */}
<div className="max-w-2xl">          {/* 最大宽度 42rem */}
<div className="min-h-screen">       {/* 最小高度 100vh */}
```

---

## 5.3 响应式设计

### 响应式断点

Tailwind CSS 使用移动优先的方法，默认样式适用于移动设备，使用前缀指定更大屏幕的样式。

**断点：**
- `sm:`：640px 及以上
- `md:`：768px 及以上
- `lg:`：1024px 及以上
- `xl:`：1280px 及以上
- `2xl:`：1536px 及以上

**示例：**

```typescript
<div className="
  grid 
  grid-cols-1        {/* 移动端：1 列 */}
  md:grid-cols-2     {/* 中等屏幕：2 列 */}
  lg:grid-cols-3     {/* 大屏幕：3 列 */}
  gap-4
">
  {/* 内容 */}
</div>
```

---

### 响应式示例

**响应式导航：**

```typescript
<nav className="
  flex 
  flex-col           {/* 移动端：垂直布局 */}
  md:flex-row         {/* 中等屏幕：水平布局 */}
  items-center
  gap-4
  p-4
">
  <a href="/">首页</a>
  <a href="/about">关于</a>
  <a href="/contact">联系</a>
</nav>
```

**响应式卡片网格：**

```typescript
<div className="
  grid 
  grid-cols-1        {/* 移动端：1 列 */}
  sm:grid-cols-2     {/* 小屏幕：2 列 */}
  lg:grid-cols-3     {/* 大屏幕：3 列 */}
  xl:grid-cols-4     {/* 超大屏幕：4 列 */}
  gap-6
">
  {/* 卡片内容 */}
</div>
```

---

## 5.4 创建样式工具函数

### 方式 1：使用 clsx 或 classnames

**安装依赖：**

```bash
npm install clsx
```

**创建工具函数：** `app/lib/utils.ts`

```typescript
// app/lib/utils.ts
import { type ClassValue, clsx } from 'clsx'

export function cn(...inputs: ClassValue[]) {
  return clsx(inputs)
}
```

**使用示例：**

```typescript
import { cn } from '@/app/lib/utils'

interface ButtonProps {
  variant?: 'primary' | 'secondary'
  disabled?: boolean
  className?: string
}

export default function Button({
  variant = 'primary',
  disabled = false,
  className,
}: ButtonProps) {
  return (
    <button
      className={cn(
        'px-4 py-2 rounded font-medium transition-colors',
        {
          'bg-blue-500 text-white hover:bg-blue-600': variant === 'primary',
          'bg-gray-200 text-gray-800 hover:bg-gray-300': variant === 'secondary',
          'opacity-50 cursor-not-allowed': disabled,
          'cursor-pointer': !disabled,
        },
        className // 允许外部传入额外的类名
      )}
    >
      按钮
    </button>
  )
}
```

---

### 方式 2：创建样式常量

**创建文件：** `app/lib/styles.ts`

```typescript
// app/lib/styles.ts
export const buttonStyles = {
  base: 'px-4 py-2 rounded font-medium transition-colors',
  variants: {
    primary: 'bg-blue-500 text-white hover:bg-blue-600',
    secondary: 'bg-gray-200 text-gray-800 hover:bg-gray-300',
    danger: 'bg-red-500 text-white hover:bg-red-600',
  },
  disabled: 'opacity-50 cursor-not-allowed',
  enabled: 'cursor-pointer',
}

export const cardStyles = {
  base: 'bg-white rounded-lg shadow-md p-6',
  hover: 'hover:shadow-lg transition-shadow',
}

export const inputStyles = {
  base: 'w-full px-4 py-2 border border-gray-300 rounded-md',
  focus: 'focus:outline-none focus:ring-2 focus:ring-blue-500',
  error: 'border-red-500 focus:ring-red-500',
}
```

**使用示例：**

```typescript
import { buttonStyles } from '@/app/lib/styles'
import { cn } from '@/app/lib/utils'

export default function Button({
  variant = 'primary',
  disabled = false,
  className,
}: ButtonProps) {
  return (
    <button
      className={cn(
        buttonStyles.base,
        buttonStyles.variants[variant],
        disabled ? buttonStyles.disabled : buttonStyles.enabled,
        className
      )}
    >
      按钮
    </button>
  )
}
```

---

## 5.5 实践：美化现有页面

### 示例 1：美化首页

**原始代码：** `app/page.tsx`

```typescript
export default function HomePage() {
  return (
    <div>
      <h1>欢迎</h1>
      <p>这是首页</p>
    </div>
  )
}
```

**美化后：**

```typescript
import Link from 'next/link'
import Button from '@/app/components/ui/Button'
import Card from '@/app/components/ui/Card'

export default function HomePage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
      {/* 头部 */}
      <header className="bg-white shadow-sm">
        <div className="container mx-auto px-4 py-6">
          <nav className="flex items-center justify-between">
            <h1 className="text-2xl font-bold text-gray-800">我的网站</h1>
            <div className="flex gap-4">
              <Link href="/about" className="text-gray-600 hover:text-gray-800">
                关于
              </Link>
              <Link href="/contact" className="text-gray-600 hover:text-gray-800">
                联系
              </Link>
            </div>
          </nav>
        </div>
      </header>
      
      {/* 主要内容 */}
      <main className="container mx-auto px-4 py-16">
        <div className="text-center mb-12">
          <h2 className="text-5xl font-bold text-gray-800 mb-4">
            欢迎来到我的网站
          </h2>
          <p className="text-xl text-gray-600 mb-8">
            这是一个使用 Next.js 和 Tailwind CSS 构建的现代化网站
          </p>
          <div className="flex gap-4 justify-center">
            <Button variant="primary">开始使用</Button>
            <Button variant="secondary">了解更多</Button>
          </div>
        </div>
        
        {/* 功能卡片 */}
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mt-16">
          <Card title="快速开发">
            <p className="text-gray-600">
              使用 Next.js 和 Tailwind CSS 快速构建现代化网站
            </p>
          </Card>
          <Card title="响应式设计">
            <p className="text-gray-600">
              完美适配各种设备，从手机到桌面电脑
            </p>
          </Card>
          <Card title="性能优化">
            <p className="text-gray-600">
              自动优化，提供最佳的用户体验
            </p>
          </Card>
        </div>
      </main>
      
      {/* 底部 */}
      <footer className="bg-gray-800 text-white mt-16">
        <div className="container mx-auto px-4 py-8 text-center">
          <p>&copy; 2024 我的网站. 保留所有权利.</p>
        </div>
      </footer>
    </div>
  )
}
```

---

### 示例 2：美化关于页面

**原始代码：** `app/about/page.tsx`

```typescript
export default function AboutPage() {
  return (
    <div>
      <h1>关于我们</h1>
      <p>这是关于页面的内容</p>
    </div>
  )
}
```

**美化后：**

```typescript
import Card from '@/app/components/ui/Card'

export default function AboutPage() {
  return (
    <div className="min-h-screen bg-gray-50">
      {/* 英雄区域 */}
      <section className="bg-gradient-to-r from-blue-600 to-indigo-700 text-white py-20">
        <div className="container mx-auto px-4 text-center">
          <h1 className="text-5xl font-bold mb-4">关于我们</h1>
          <p className="text-xl text-blue-100 max-w-2xl mx-auto">
            了解我们的故事、使命和价值观
          </p>
        </div>
      </section>
      
      {/* 内容区域 */}
      <section className="container mx-auto px-4 py-16">
        <div className="max-w-4xl mx-auto">
          <Card title="我们的故事" className="mb-8">
            <p className="text-gray-700 leading-relaxed mb-4">
              我们是一家致力于创新的公司，成立于 2020 年。
              我们的使命是通过技术改变世界，为用户提供最好的产品和服务。
            </p>
            <p className="text-gray-700 leading-relaxed">
              我们相信，通过持续的努力和创新，我们可以创造一个更美好的未来。
            </p>
          </Card>
          
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            <Card title="我们的使命">
              <p className="text-gray-700">
                通过技术创新，为用户提供卓越的产品和服务体验。
              </p>
            </Card>
            <Card title="我们的愿景">
              <p className="text-gray-700">
                成为行业领先的技术公司，推动社会进步。
              </p>
            </Card>
          </div>
        </div>
      </section>
    </div>
  )
}
```

---

### 示例 3：美化联系页面

**美化后：** `app/contact/page.tsx`

```typescript
'use client'

import { useState } from 'react'
import Card from '@/app/components/ui/Card'
import Input from '@/app/components/ui/Input'
import Button from '@/app/components/ui/Button'

export default function ContactPage() {
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    message: '',
  })
  
  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault()
    console.log('提交:', formData)
  }
  
  return (
    <div className="min-h-screen bg-gray-50">
      {/* 头部 */}
      <section className="bg-white shadow-sm">
        <div className="container mx-auto px-4 py-12 text-center">
          <h1 className="text-4xl font-bold text-gray-800 mb-4">联系我们</h1>
          <p className="text-gray-600 max-w-2xl mx-auto">
            有任何问题或建议？我们很乐意听到您的声音
          </p>
        </div>
      </section>
      
      {/* 表单区域 */}
      <section className="container mx-auto px-4 py-16">
        <div className="max-w-2xl mx-auto">
          <Card>
            <form onSubmit={handleSubmit} className="space-y-6">
              <Input
                label="姓名"
                value={formData.name}
                onChange={(e) => setFormData({ ...formData, name: e.target.value })}
                required
              />
              <Input
                label="邮箱"
                type="email"
                value={formData.email}
                onChange={(e) => setFormData({ ...formData, email: e.target.value })}
                required
              />
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-2">
                  留言
                </label>
                <textarea
                  value={formData.message}
                  onChange={(e) => setFormData({ ...formData, message: e.target.value })}
                  required
                  rows={6}
                  className="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                />
              </div>
              <div className="flex justify-end">
                <Button type="submit" variant="primary">
                  发送消息
                </Button>
              </div>
            </form>
          </Card>
          
          {/* 联系信息 */}
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mt-12">
            <Card className="text-center">
              <div className="text-3xl mb-2">📧</div>
              <h3 className="font-semibold mb-2">邮箱</h3>
              <p className="text-gray-600 text-sm">contact@example.com</p>
            </Card>
            <Card className="text-center">
              <div className="text-3xl mb-2">📞</div>
              <h3 className="font-semibold mb-2">电话</h3>
              <p className="text-gray-600 text-sm">+86 123 456 7890</p>
            </Card>
            <Card className="text-center">
              <div className="text-3xl mb-2">📍</div>
              <h3 className="font-semibold mb-2">地址</h3>
              <p className="text-gray-600 text-sm">北京市朝阳区</p>
            </Card>
          </div>
        </div>
      </section>
    </div>
  )
}
```

---

## 5.6 Tailwind CSS 高级技巧

### 技巧 1：使用 @apply 指令

**创建自定义组件类：** `app/globals.css`

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer components {
  .btn-primary {
    @apply px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition-colors;
  }
  
  .btn-secondary {
    @apply px-4 py-2 bg-gray-200 text-gray-800 rounded hover:bg-gray-300 transition-colors;
  }
  
  .card {
    @apply bg-white rounded-lg shadow-md p-6;
  }
}
```

**使用：**

```typescript
<button className="btn-primary">主要按钮</button>
<button className="btn-secondary">次要按钮</button>
<div className="card">卡片内容</div>
```

---

### 技巧 2：自定义主题配置

**配置文件：** `tailwind.config.ts`

```typescript
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#eff6ff',
          100: '#dbeafe',
          // ... 更多色阶
          500: '#3b82f6',
          // ... 更多色阶
          900: '#1e3a8a',
        },
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
      },
      spacing: {
        '128': '32rem',
        '144': '36rem',
      },
    },
  },
  plugins: [],
}
export default config
```

---

### 技巧 3：使用 dark 模式

**配置 dark 模式：** `tailwind.config.ts`

```typescript
const config: Config = {
  darkMode: 'class', // 或 'media'
  // ... 其他配置
}
```

**使用：**

```typescript
<div className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white">
  内容
</div>
```

**切换 dark 模式：**

```typescript
'use client'

import { useEffect, useState } from 'react'

export default function ThemeToggle() {
  const [dark, setDark] = useState(false)
  
  useEffect(() => {
    if (dark) {
      document.documentElement.classList.add('dark')
    } else {
      document.documentElement.classList.remove('dark')
    }
  }, [dark])
  
  return (
    <button onClick={() => setDark(!dark)}>
      {dark ? '🌙' : '☀️'}
    </button>
  )
}
```

---

## 5.7 样式管理最佳实践

### 实践 1：使用工具函数组合类名

```typescript
// ✅ 推荐：使用 cn 函数
import { cn } from '@/app/lib/utils'

<button className={cn(
  'base-class',
  variant === 'primary' && 'primary-class',
  disabled && 'disabled-class',
  className
)}>
  按钮
</button>

// ❌ 不推荐：手动拼接字符串
<button className={`base-class ${variant === 'primary' ? 'primary-class' : ''} ${disabled ? 'disabled-class' : ''}`}>
  按钮
</button>
```

### 实践 2：创建可复用的样式常量

```typescript
// ✅ 推荐：集中管理样式
export const styles = {
  button: {
    base: 'px-4 py-2 rounded',
    primary: 'bg-blue-500 text-white',
    secondary: 'bg-gray-200 text-gray-800',
  },
}

// ❌ 不推荐：到处重复写样式类名
<button className="px-4 py-2 rounded bg-blue-500 text-white">按钮1</button>
<button className="px-4 py-2 rounded bg-blue-500 text-white">按钮2</button>
```

### 实践 3：保持响应式设计

```typescript
// ✅ 推荐：移动优先的响应式设计
<div className="
  grid 
  grid-cols-1 
  md:grid-cols-2 
  lg:grid-cols-3
">
  内容
</div>

// ❌ 不推荐：只考虑桌面端
<div className="grid grid-cols-3">
  内容
</div>
```

---

## 5.8 常见问题解答

### Q1：Tailwind CSS 会增加 CSS 体积吗？

**A：** 不会。Tailwind CSS 使用 PurgeCSS（现在叫 content scanning）自动移除未使用的样式，最终生成的 CSS 文件很小。

### Q2：可以自定义 Tailwind 的配置吗？

**A：** 可以。在 `tailwind.config.ts` 中可以自定义颜色、字体、间距等。

### Q3：如何处理复杂的样式？

**A：** 对于复杂的样式，可以使用：
- `@apply` 指令创建组件类
- CSS Modules
- 内联样式（不推荐，但有时必要）

---

## 5.9 总结

### 关键要点

1. **Tailwind CSS 基础**：使用工具类快速构建界面
2. **响应式设计**：使用断点前缀实现响应式布局
3. **样式工具函数**：使用 `clsx` 或 `classnames` 组合类名
4. **美化页面**：使用 Tailwind CSS 美化现有页面
5. **最佳实践**：集中管理样式、保持响应式、使用工具函数

### 下一步

完成样式管理学习后，第三阶段：组件开发就完成了。可以进入第四阶段：**数据获取**，学习如何在服务端组件中获取数据。

---

**学习进度：** ✅ 样式管理学习完成

