# 第一阶段：基础概念

## 1. Next.js 简介与环境搭建

> 📖 **详细讲解**：请查看 [01-Next.js简介与环境搭建.md](./01-Next.js简介与环境搭建.md)

本文档包含 Next.js 简介和环境搭建的完整内容，涵盖：

- Next.js 是什么：核心特性和功能
- 环境搭建：前置要求、创建项目、启动开发服务器

### 快速要点

- Next.js 是基于 React 的全栈框架
- 提供 SSR、SSG、API 路由等功能
- 使用 `create-next-app` 快速创建项目

---

## 2. 项目结构理解

> 📖 **详细讲解**：请查看 [02-项目结构理解.md](./02-项目结构理解.md)

本文档包含项目结构的完整讲解，涵盖：

- 核心目录结构：`app/`、`public/` 等
- 规范的目录结构详解：完整的推荐目录结构
- 核心目录详解：`app/`、`components/`、`lib/`、`types/`、`api/`、`hooks/`、`public/` 等
- 特殊文件说明：Next.js 特殊文件的作用和位置
- 组件组织最佳实践：如何判断组件应该放在哪里
- 导入路径规范：使用路径别名和相对路径
- 命名规范：组件、工具函数、文件的命名规则
- 常见问题解答：关于项目结构的常见疑问

### 快速要点

- `app/` 目录是 App Router 的核心，文件夹结构 = 路由路径
- `app/components/` 存放共享组件，路由文件夹内存放局部组件
- 使用路径别名 `@/` 提高代码可读性

---

## 3. 路径别名配置

> 📖 **详细讲解**：请查看 [03-路径别名配置.md](./03-路径别名配置.md)

本文档包含路径别名配置的完整内容，涵盖：

- 默认路径别名：`@/` 别名的使用
- 配置路径别名：在 `tsconfig.json` 中配置
- 使用路径别名的好处：代码清晰、易于重构

### 快速要点

- Next.js 默认配置了 `@/` 别名，指向项目根目录
- 路径别名在 `tsconfig.json` 中配置
- 使用路径别名可以避免复杂的相对路径

---

## 4. App Router vs Pages Router

> 📖 **详细讲解**：请查看 [04-App Router vs Pages Router.md](./04-App%20Router%20vs%20Pages%20Router.md)

本文档包含 App Router 和 Pages Router 的对比，涵盖：

- 为什么使用 App Router：性能、灵活性、数据获取等优势
- App Router vs Pages Router 对比：特性对比表
- 文件系统路由工作原理：文件夹结构 = 路由路径

### 快速要点

- App Router 是 Next.js 13+ 推荐的新架构
- 支持 React Server Components、嵌套布局、流式渲染等
- 文件夹结构直接对应路由路径

---

## 5. 服务端组件 vs 客户端组件

> 📖 **详细讲解**：请查看 [05-服务端组件 vs 客户端组件.md](./05-服务端组件%20vs%20客户端组件.md)

本文档包含服务端组件和客户端组件的完整讲解，涵盖：

- 默认：服务端组件：默认组件类型、特点
- 客户端组件：使用 `'use client'` 指令
- 何时使用哪种组件：使用场景对比
- 实践：创建服务端和客户端组件示例

### 快速要点

- 默认所有组件都是服务端组件
- 需要交互性时使用 `'use client'` 创建客户端组件
- 服务端组件用于获取数据，客户端组件用于交互

---

## 6. 理解 Layout 和 Page

> 📖 **详细讲解**：请查看 [06-Layout 和 Page 详细讲解.md](./06-Layout%20和%20Page%20详细讲解.md)

本文档包含 Layout 和 Page 的完整讲解，涵盖：

- Layout（布局）详解：基本概念、关键特点、根 Layout 的特殊要求
- Page（页面）详解：基本概念、关键特点、数据获取方式
- Layout 和 Page 的区别：对比表、渲染时机、使用场景
- 嵌套布局的工作原理：嵌套概念、文件结构、渲染顺序、完整示例
- 每个路由文件夹的 Layout 和 Page：核心规则、实际示例
- 实际应用示例：博客网站、电商网站、完整代码示例
- Layout 和 Page 之间的通信：Context API、URL 参数、状态管理库等方法
- 最佳实践：何时使用嵌套 Layout、Layout 和 Page 的最佳实践、性能优化建议

### 快速要点

- **Layout**：共享的布局结构，路由切换时不会重新渲染，会嵌套
- **Page**：页面的具体内容，路由切换时会重新渲染，不会嵌套
- **每个路由文件夹**：都可以有自己的 `page.tsx` 和 `layout.tsx`

---

## 7. 总结

### 关键概念回顾

1. **App Router**：Next.js 13+ 的新路由系统，基于 `app/` 目录
2. **服务端组件**：默认组件类型，在服务器上运行，减少客户端 JavaScript
3. **客户端组件**：使用 `'use client'`，支持交互性和 React Hooks
4. **Layout**：共享的布局结构，路由切换时不重新渲染
5. **Page**：页面的具体内容，每个路由必须有一个 `page.tsx`

### 最佳实践

- ✅ 默认使用服务端组件，只在需要交互时使用客户端组件
- ✅ 使用路径别名 `@/` 提高代码可读性
- ✅ 合理使用嵌套布局组织页面结构
- ✅ 在服务端组件中获取数据，减少客户端负担

### 下一步

完成第一阶段后，可以进入第二阶段：**路由系统**，学习静态路由、动态路由、路由组等高级路由功能。

---

**学习进度：** ✅ 第一阶段完成
